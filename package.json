{
  "name": "web-spider-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "check": "npm run check --workspaces",
    "postinstall": "find node_modules -path '*/tr46/index.js' -exec sed -i 's|require(\"punycode/\")|require(\"punycode\")|g' {} +"
  },
  "pi": {
    "extensions": [
      "packages/pi-extension/src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
